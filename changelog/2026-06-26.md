# Changelog — 2026-06-26

## VS-01 Step 5: Progress Update

- В `src/domain/workflow.ts` добавлена pure операция обновления Progress для переходов `not_started -> in_progress` и `not_started -> requires_check`.
- Операция изменяет только выбранный Progress, сохраняет Action Plan в состоянии `active` и добавляет append-only History Event `progress_status_changed` с previous/new status и versioned step context.
- В `src/app/App.tsx` добавлены два явных Progress-контрола в Step Detail после полного контекста шага; Progress продолжает отображаться как `Ваша отметка`, а History пользователю не показывается.
- Обновлены `src/app/App.css`, `src/domain/workflow.test.ts` и `src/app/App.test.tsx`: покрыты оба перехода, неизменность остальных Progress, History payload, запрет повторного Step 5 перехода, UI controls и отсутствие History view.
- Обновлён `plans/active/2026-06-22-vs-01-implementation-plan.md`: Step 5 отмечен как completed без расширения scope.

## VS-01 Step 6: History Visibility

- В `src/app/App.tsx` добавлен read-only History view для существующего локального Action Plan с событиями `action_plan_created` и `progress_status_changed`.
- События показываются в хронологическом порядке с понятным типом, описанием, временем и связанным шагом для Progress update.
- History явно обозначена как внутренняя история Nova Agent, не официальный журнал и не подтверждение выполнения действий пользователем.
- В `src/app/App.css` добавлено адаптивное представление хронологии без task-manager или dashboard-паттернов.
- В `src/app/App.test.tsx` добавлены проверки обоих event types, порядка, step context, timestamps, boundary copy и отсутствия edit/delete/filter/search/sort controls.
- Обновлён `plans/active/2026-06-22-vs-01-implementation-plan.md`: Step 6 отмечен как completed без расширения scope.

## VS-01 Step 7: Demo Flow Validation

- В `src/app/App.test.tsx` добавлен один полный interaction test для VS-01 demo flow: Life Situation → Scenario → Start Action Plan → Action Plan Detail → Step Detail → Progress Update → History.
- Тест проходит через пользовательские действия над существующим UI: нажимает `Начать план`, открывает шаг, применяет разрешённое Progress-обновление и открывает History.
- Проверены Warnings/Restrictions до CTA, active Action Plan, маркировка Progress как `Ваша отметка`, события History и boundary copy внутреннего неофициального журнала.
- Зафиксирован testing note: текущий Demo Flow interaction test использует React internal dispatcher workaround из-за Vitest `node` setup; это временный test-only компромисс, не permanent testing strategy, не добавляет runtime-код и требует рассмотрения normal DOM interaction test setup перед VS-02.
- Обновлён `plans/active/2026-06-22-vs-01-implementation-plan.md`: Step 7 отмечен как completed без новых функций, refactor, VS-02 scope или переноса плана в completed.

## VS-01 Cleanup

- `src/app/App.tsx` сокращён до orchestration layer: локальное состояние, выбор текущего view и вызовы существующих workflow helpers.
- Существующие VS-01 view-компоненты перенесены в `src/app/components/` без изменения пользовательского поведения, UI scope или CSS-классов.
- В `src/data/contentRepository.ts` добавлен lookup content flow по `ActionPlan.scenarioVersionId` и selected Life Situation context; активный план больше не восстанавливает Scenario через первый элемент каталога.
- `README.md` обновлён как developer entrypoint: назначение Nova Agent, проблема, архитектура верхнего уровня, команды запуска/проверки, структура репозитория и ссылки на основные документы.
- В `plans/active/2026-06-22-vs-01-implementation-plan.md` зафиксирована VS-01 Cleanup phase и будущий Git workflow: `main -> feature branch -> commit -> push -> merge -> main`.
- Scope сохранён: новые пользовательские функции, VS-02, Product Principles, TS01-TS08, UI-01, Supabase, API handlers, auth, routing library, state manager и новые зависимости не добавлялись.

## VS-02 Implementation Plan Preparation

- Создан `plans/active/2026-06-26-vs-02-implementation-plan.md` для VS-02 Return and Continue Context.
- План фиксирует, что VS-02 является локальным continuation slice для существующего active Action Plan, а не My Plans dashboard, Completed Plans или persistence/auth/API этапом.
- Зафиксированы границы: next step определяется только через Progress, History не используется как source of truth, reload survival/persistence/auth/Supabase/API handlers отсутствуют как сознательное ограничение VS-02.
- Scope creep явно запрещён для User Open Questions, User Notes, Checked Source Marks, Pattern B, Content Admin, dashboard, filters/search/sorting, task-manager элементов, routing library, state manager и document storage.
- Уточнены Progress Summary, единственный continuation entry и explicit forbidden checks для dashboard/deadlines/priorities/assignees/kanban/document storage/productivity metrics.
