# Changelog — 2026-06-27

## VS-02 Step 2: Return Context UI

- Добавлен `src/app/components/ReturnContextView.tsx` как единственный active-only continuation entry для существующего локального Action Plan.
- В `src/app/App.tsx` добавлен переход `Return Context -> active Action Plan / Step Detail / History` без routing library, state manager, persistence, API handlers или Supabase.
- Return Context использует `getVs02ProgressSummary` и `getVs02NextStepProgress`; next step определяется только через Progress records, History не используется как source of truth.
- Progress Summary показывает только total steps, `not_started`, `in_progress` и `requires_check`; completed count, percentages, KPI, productivity metrics и dashboard fields не добавлены.
- В `src/app/App.test.tsx` добавлены проверки continuation entry, summary fields, next step from Progress, переходов в Step Detail/History и отсутствия dashboard/search/filter/sorting/completed-plan scope.
- Обновлён `plans/active/2026-06-26-vs-02-implementation-plan.md`: Step 2 отмечен как completed без User Open Questions, User Notes, Checked Source Marks, Pattern B, Content Admin, auth или document storage.
