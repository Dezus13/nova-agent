# Changelog — 2026-06-28

## VS-02 Step 3: Demo Flow Validation

- Added one full interaction test in `src/app/App.test.tsx` for the VS-02 return-and-continue demo flow: Return Context, Progress Summary, Next Step, active Action Plan, Step Detail, return to plan and History.
- The test uses existing UI click handlers and validates that Progress Summary stays limited to total steps, `not_started`, `in_progress` and `requires_check`.
- The test asserts that the next step is based on Progress records, History remains read-only and internal, reload persistence is not promised, and forbidden dashboard/task-manager/persistence/API/Supabase scope is absent.
- Updated `plans/active/2026-06-26-vs-02-implementation-plan.md` to mark Step 3 as completed without adding runtime functionality or expanding VS-02 scope.

## VS-02 Final Audit Fix

- Updated `src/app/components/ActionPlanView.tsx` to use `getVs02NextStepProgress` instead of a local first-non-completed rule.
- Added a regression test in `src/app/App.test.tsx` confirming Return Context and Action Plan Detail show the same next step when earlier Progress is `not_started`, later Progress is `requires_check`, and a later step is `in_progress`.
- Kept History out of next-step calculation and did not add dashboard, completed plans, persistence, API, Supabase, auth, routing or state manager scope.

## VS-02 Closure

- Officially closed VS-02 as completed after final audit.
- Moved `plans/active/2026-06-26-vs-02-implementation-plan.md` to `plans/completed/`.
- Updated `plans/active/2026-06-21-mvp-work-plan.md` to mark VS-01 and VS-02 completed and identify VS-03 as the next stage.

## VS-01 Active Plan Cleanup

- Moved `plans/active/2026-06-22-vs-01-implementation-plan.md` to `plans/completed/`.
- Added final VS-01 closure status with preserved scope and known local-only limitations.
- Cleaned `plans/active/` so completed VS-01 and VS-02 implementation plans now live in `plans/completed/`.

## VS-03 Preparation

- Created `plans/active/2026-06-28-vs-03-implementation-plan.md` for the User Open Questions vertical slice.
- Defined VS-03 as one active vertical slice for one user problem: preserving uncertainty as a user-owned question for external verification.
- Kept Product Principles, MVP Work Plan, TS01-TS08, UI-01, README, AGENTS and application code unchanged.

## VS-03 Step 1: User Open Question Domain Layer

- Added local User Open Question domain types and TS07 status allowlist in `src/domain/workflow.ts`.
- Added a pure create helper for User Open Questions inside an existing active Action Plan without UI, repository, API, persistence or workflow side effects.
- Added unit tests for creation, approved statuses, Progress independence, History independence and existing Action Plan aggregate immutability.
- Updated `plans/active/2026-06-28-vs-03-implementation-plan.md` to mark Step 1 completed and keep status/edit/history behavior in later VS-03 steps.

## VS-03 Step 2: User Open Questions Read-Only UI

- Added a minimal `UserOpenQuestionsView` inside the existing Action Plan Detail context.
- Displayed an empty state for no questions and a read-only list for existing User Open Questions with text, TS07 status, "Ваш вопрос" label and external-verification boundary copy.
- Added tests for empty/list states, visible user-question labeling, boundary copy and absence of edit/delete/status update, User Notes, Checked Source Marks, answers and AI suggestions.
- Updated `plans/active/2026-06-28-vs-03-implementation-plan.md` to mark Step 2 completed as read-only UI with create/edit/status/history behavior still out of scope for this step.

## VS-03 Step 3: User Open Question Creation

- Added minimal Action Plan UI for entering a new User Open Question and clicking "Добавить вопрос".
- Created new questions through the existing `createUserOpenQuestion` domain helper with initial status `open`, then displayed them in the current active-plan list.
- Kept Progress, History and the existing Action Plan aggregate unchanged; no status update, edit, delete, History Event, User Notes, Checked Source Marks, answers, AI suggestions, API or persistence behavior was added.
- Added interaction coverage for creation, input clearing, `open` status, list display and absence of forbidden controls or answer-like UI.

## VS-03 Step 4: User Open Question Status Update

- Added a pure `updateUserOpenQuestionStatus` helper that enforces TS07 status values and lifecycle transitions, including final `irrelevant`.
- Added minimal status controls for existing User Open Questions inside the active Action Plan context.
- Kept Progress, History and the existing Action Plan aggregate unchanged; no text edit, delete, History Event, User Notes, Checked Source Marks, API or persistence behavior was added.
- Added tests for successful status update, invalid runtime status rejection, forbidden transitions, unchanged question text and absence of workflow side effects.

## VS-03 Step 5: User Open Question History Events

- Added append-only UOQ History Events for `user_open_question_created` and `user_open_question_status_changed`.
- Updated local App orchestration so UOQ creation and status changes append internal Nova Agent History while keeping Progress and Action Plan state unchanged.
- Extended the existing read-only `HistoryView` formatting for UOQ events without adding a new History UI, edit/delete controls, User Notes, Checked Source Marks, API, Supabase, auth or persistence behavior.
- Updated tests for UOQ History creation, append-only behavior, Progress independence, Action Plan state independence and non-official History boundary copy.

## VS-03 Step 6: Demo Flow Validation

- Added one full interaction test for the VS-03 User Open Question flow in `src/app/App.test.tsx`.
- Validated existing active Action Plan context, User Open Question creation, initial `open` status, allowed status update, UOQ History Events, internal History boundary copy, unchanged Progress and `active` Action Plan state.
- Updated `plans/active/2026-06-28-vs-03-implementation-plan.md` to mark Step 6 completed as validation-only without adding edit/delete, User Notes, Checked Source Marks, Dashboard, Completed Plans, Supabase, API, auth, routing, state manager, persistence or document storage.

## VS-03 Closure

- Officially closed VS-03 as completed after demo flow validation.
- Moved `plans/active/2026-06-28-vs-03-implementation-plan.md` to `plans/completed/`.
- Updated `plans/active/2026-06-21-mvp-work-plan.md` to mark VS-03 completed and identify VS-04 as the next vertical slice.
- Kept application code, specs, Product Principles, Technical Architecture, README and AGENTS unchanged.

## VS-04 Preparation

- Created `plans/active/2026-06-28-vs-04-implementation-plan.md` for the Sources And Notes vertical slice.
- Defined VS-04 as active-plan-only Checked Source Marks and User Notes with strict boundaries: no source verification by Nova Agent, no official status, no document storage, no notes app, no CRM and no task-manager behavior.
- Kept Product Principles, MVP Work Plan, TS01-TS08, UI-01, Decision Records, Technical Architecture, README, AGENTS and application code unchanged.

## VS-04 Step 1: Checked Source Mark Domain Layer

- Added a local Checked Source Mark domain type and pure create helper in `src/domain/workflow.ts` for existing active Action Plans only.
- Added unit tests for creating a user-owned source mark, preserving Progress, History and Action Plan state, avoiding in-place aggregate mutation and avoiding User Notes or official verification fields.
- Updated `plans/active/2026-06-28-vs-04-implementation-plan.md` to mark Step 1 completed and defer duplicate/idempotency enforcement until Checked Source Marks are represented in local list state.

## VS-04 Step 2: Checked Source Mark UI

- Added local active-plan UI state for Checked Source Marks and displayed the action next to existing step sources.
- Added the "Отметить как проверено мной" action and "Отмечено вами" state using the existing `createCheckedSourceMark` helper.
- Added simple local duplicate prevention for the same Action Plan and Source Revision without adding History Events, source verification, official status, uploads, document storage, API, Supabase, auth, routing, state manager or persistence.
- Added interaction coverage for boundary copy, idempotent marking, unchanged Progress, unchanged History, unchanged Action Plan state and absence of User Notes or official verification language.

## VS-04 Step 3: Checked Source Mark History Event

- Added the local `source_checked` History Event type and a `createCheckedSourceMarkWithHistory` helper that appends one internal event when a new Checked Source Mark is created.
- Updated App orchestration so repeated local marks for the same Action Plan and Source Revision do not create duplicate marks or duplicate `source_checked` events.
- Extended the existing read-only `HistoryView` to show `source_checked` as "Источник отмечен вами" with boundary copy that Nova Agent does not check the source, this is not an official status and this is not action confirmation.
- Added tests for append-only source mark History, duplicate prevention, unchanged Progress, unchanged Action Plan state, unchanged User Open Questions and absence of User Notes or source verification language.

## VS-04 Step 4: User Note Domain Layer

- Added the local User Note domain type and pure `createUserNote` helper for existing active Action Plans only.
- Validated that User Notes attach to an existing context History Event from the same Action Plan while staying user-owned through `createdByUser: true`.
- Kept Progress, History, Action Plan state, User Open Questions and Checked Source Marks unchanged; no UI, edit/hide/delete lifecycle, User Note History Events, source/document records, official status fields, API, Supabase, auth or persistence behavior was added.
- Added domain tests for creation, context validation, aggregate immutability, no workflow side effects and absence of Nova Agent answer/source/document/official verification fields.

## VS-04 Step 5: User Note Create/Read UI

- Added local User Note UI state and create/read controls next to existing History context events inside the active Action Plan.
- Used the existing `createUserNote` domain helper so notes attach to an existing `historyEventId` and appear as "Ваша заметка".
- Added required boundary copy: "Не официальный документ", "Не источник" and "Не ответ Nova Agent"; input clears after successful creation.
- Kept Progress, History, Action Plan state, User Open Questions and Checked Source Marks unchanged; no edit/hide/delete lifecycle, `user_note_created`, User Note History Events, source/document records, API, Supabase, auth, routing, state manager or persistence behavior was added.
- Added interaction coverage for note creation, context attachment, input clearing, unchanged workflow state and absence of forbidden controls or answer-like language.

## VS-04 Step 6: User Note History Event

- Added the local `user_note_created` History Event type and `createUserNoteWithHistory` helper while keeping the base `createUserNote` helper pure.
- Updated App orchestration so creating a User Note appends one internal audit event without changing Progress, Action Plan state, User Open Questions or Checked Source Marks.
- Updated the existing read-only `HistoryView` to display `user_note_created` as "Заметка добавлена вами" with boundary copy that it is not an official document, not a source and not a Nova Agent answer.
- Kept `user_note_created` out of User Note context targets and did not add edit/hide/archive/delete lifecycle, source/document records, API, Supabase, auth, routing, state manager, persistence or document storage.
- Added tests for append-only note History, pure helper behavior, History UI boundary copy and absence of forbidden lifecycle or answer-like language.

## VS-04 Step 7: User Note Lifecycle Gate

- Deferred User Note edit/hide/archive/delete lifecycle out of VS-04 to avoid scope creep, privacy/data retention ambiguity, API/backend decisions and document-storage drift.
- Kept VS-04 focused on Checked Source Marks, User Note create/read and their History Events, with Demo Flow Validation as the next step.
- Did not change application code, specs, Product Principles, Technical Architecture, Supabase, API, auth, persistence or document storage behavior.

## VS-04 Step 8: Demo Flow Validation

- Added one full interaction test for the VS-04 Sources And Notes flow: existing active Action Plan, Step Detail source mark, `source_checked`, User Note creation, `user_note_created` and History boundary copy.
- Verified Progress remains unchanged, Action Plan state remains `active`, User Open Questions remain unchanged and `user_note_created` cannot become a context event for another note.
- Verified absence of edit/hide/archive/delete note lifecycle, source verification, official status, document storage, source/document records, AI answer, Supabase, API, auth, routing, state manager, persistence, Dashboard, Completed Plans, Pattern B and Content Admin.
- Updated `plans/active/2026-06-28-vs-04-implementation-plan.md` to mark Step 8 completed as validation-only without runtime code changes.

## VS-04 Closure

- Closed VS-04 Sources And Notes as a completed vertical slice.
- Moved `plans/active/2026-06-28-vs-04-implementation-plan.md` to `plans/completed/2026-06-28-vs-04-implementation-plan.md`.
- Updated `plans/active/2026-06-21-mvp-work-plan.md` to mark VS-04 completed and keep VS-05 as the next vertical slice.
- Recorded the VS-04 completion summary: Checked Source Mark domain/UI, `source_checked`, User Note domain/create-read UI, `user_note_created`, lifecycle defer decision and Demo Flow Validation.
- Kept User Note edit/hide/archive/delete deferred to a later slice after persistence, backend, auth and privacy decisions.
- Did not change application code, specs, Product Principles, Technical Architecture, README, AGENTS.md, Supabase, API, auth, routing, state manager, persistence or document storage behavior.

## MVP Online Demo Plan

- Created `plans/active/2026-06-28-mvp-online-demo-plan.md` to prepare Nova Agent for a Vercel Static Demo and a gated Supabase Minimal Persistence Foundation.
- Captured the current local-only storage model, Vercel static demo settings, smoke-test checklist, minimal persistence scope, Auth / Ownership Decision Gate, testing strategy and risks.
- Did not change application code, dependencies, env files, Supabase code, Vercel config, specs, Product Principles, Technical Architecture, README or AGENTS.md.

## Demo Polish Plan

- Created `plans/active/2026-06-28-demo-polish-plan.md` to prepare the current Nova Agent MVP demo for visual polish before broader showing.
- Limited Demo Polish to visual, copy, layout, README/demo instructions and smoke-test preparation while preserving existing product logic and boundary copy.
- Explicitly kept new features, workflow/domain changes, Supabase, API, auth, routing, state manager, persistence, dashboard, completed plans management, Content Admin and document storage out of scope.

## Demo Polish Step 1: UI Visual Polish

- Polished the current app UI as a visual/copy/layout-only step: clearer first screen, calmer section hierarchy, improved card spacing, friendlier user-facing labels and stronger visual separation for History, User Notes, Sources and Checked Source Marks.
- Kept boundary copy visible while making risky-action areas easier to scan: official-status, source-checking, action-confirmation, User Note and local-only limitations remain explicit.
- Updated only presentation-layer code, tests, the active Demo Polish plan and changelog; workflow helpers, domain logic, Progress source of truth, History semantics, UOQ/CSM/User Note behavior, Supabase, API, auth, routing, state manager and persistence were not changed.

## Demo Polish Step 2: README / Demo Instructions

- Updated `README.md` for the current Nova Agent MVP demo: online Vercel URL, VS-01 through VS-04 completion state, Demo Polish Step 1 completion, current demo capabilities, local run commands and build/test commands.
- Documented demo limitations and product boundaries: local-only state, reload state loss, no Supabase/auth/persistence, no official status, no action confirmation, no source verification and User Notes are not documents, sources or Nova Agent answers.
- Marked the README/demo instructions step completed in `plans/active/2026-06-28-demo-polish-plan.md`.
- Did not change application code, package configuration, specs, Product Principles, Technical Architecture, AGENTS.md, Supabase, API, auth, routing, state manager or persistence behavior.

## Demo Polish Closure

- Closed Demo Polish as completed after UI polish, README/demo instructions and local + Vercel smoke validation.
- Moved `plans/active/2026-06-28-demo-polish-plan.md` to `plans/completed/2026-06-28-demo-polish-plan.md`.
- Confirmed the demo was ready at the old Vercel project / outdated deployment `https://nova-agent-teal.vercel.app/` at the time of Demo Polish closure; do not use it for the current demo.
- Did not change application code, README, specs, Product Principles, Technical Architecture, Supabase, API, auth, routing, state manager, persistence, dashboard, completed plans management, Content Admin or document storage behavior.

## Agentic Demo Shell Plan

- Created `plans/active/2026-06-28-agentic-demo-shell-plan.md` to prepare a demo-only AI-product-style entry layer before the existing scenario.
- Limited the shell to a deterministic UI/demo mapping from a user prompt to the existing seed scenario and current Action Plan workflow.
- Explicitly kept real AI integration, OpenAI API, Supabase, auth, routing, persistence, new domain logic, workflow helper changes, scenario generation, search, dashboard, CRM/task-manager behavior and document storage out of scope.
- Did not change application code, README, specs, Product Principles, Technical Architecture, package configuration, env files, Supabase, API, auth, routing, state manager or persistence behavior.

## Agentic Demo Shell Step 1: UI-Only Implementation

- Added a demo-only first screen before the existing workflow with task input, example prompt, "Построить план", demo summary and "Открыть план" transition.
- Kept the mapping deterministic: any non-empty demo prompt opens the current seed scenario and existing Action Plan flow.
- Added boundary copy that the flow uses the current demonstration scenario and that Nova Agent does not create new scenarios in this version.
- Updated tests to cover the shell first screen, empty-input guard, demo-only summary and the existing VS-01 through VS-04 flow after transition.
- Did not change domain logic, workflow helpers, content repository, Progress, History, User Open Questions, Checked Source Marks, User Notes, OpenAI, Supabase, API, auth, routing, state manager or persistence behavior.

## Agentic Demo Shell Step 2: README / Demo Instructions

- Updated `README.md` to describe the Agentic Demo Shell as a demo-only first layer before the existing workflow.
- Documented the sample-task input, demo-only summary, deterministic mapping from any non-empty prompt to the current seed scenario and the existing Action Plan flow.
- Documented that real AI, OpenAI API, real intent recognition, universal scenario search, scenario generation and production assistant behavior are not implemented.
- Preserved the online demo URL and static-demo limitations: local-only state, reload state loss, no Supabase, no auth and no persistence.

## Agentic Demo Production URL Update

- Agentic Demo production URL updated to `https://nova-agent-demo.vercel.app/`.
- Old `https://nova-agent-teal.vercel.app/` deployment is no longer the current demo URL.
- Updated documentation only; application code, product logic, package configuration, specs, Product Principles, Technical Architecture, AGENTS.md, tests, Supabase, API, auth, routing, state manager and persistence were not changed.
- Did not change application code, tests, package configuration, specs, Product Principles, Technical Architecture, AGENTS.md, OpenAI, Supabase, API, auth, routing, state manager or persistence behavior.

## Agentic Demo Shell Closure

- Closed Agentic Demo Shell as completed after UI-only implementation, README/demo instructions update, production smoke validation and production URL update.
- Moved `plans/active/2026-06-28-agentic-demo-shell-plan.md` to `plans/completed/2026-06-28-agentic-demo-shell-plan.md`.
- Confirmed the current production demo is ready at `https://nova-agent-demo.vercel.app/`.
- Confirmed real AI, OpenAI API, Supabase, auth, routing, state manager, persistence, scenario generation and new domain logic were not added.
- Did not change application code, README, package configuration, specs, Product Principles, Technical Architecture, AGENTS.md, workflow/domain logic or tests.

## AI Demo Presentation Layer Plan

- Created `plans/active/2026-06-28-ai-demo-presentation-layer-plan.md` to plan a presentation-only AI-feel layer on top of the existing Agentic Demo flow.
- Limited the plan to UI/presentation changes that preserve deterministic mapping to the current seed scenario and existing Action Plan workflow.
- Explicitly kept real AI, OpenAI API, backend API, Supabase, auth, routing, persistence, new domain logic, scenario generation, source verification, document upload and dashboard behavior out of scope.
- Did not change application code, README, package configuration, specs, Product Principles, Technical Architecture, AGENTS.md, tests, workflow/domain logic or content repository behavior.

## AI Demo Presentation Layer Step 1: UI Implementation

- Updated the first-screen presentation to feel more like an AI-assistant demo while keeping it demo-only and mapped to the existing seed scenario.
- Added a larger life-task input, clearer assistant-style response card and visible boundary copy for current demonstration scenario, no new scenarios, no real AI/OpenAI and no legal/medical/tax advice.
- Updated tests for the new visible copy and structure without changing domain logic, workflow helpers, content repository, Progress, History, User Open Questions, Checked Source Marks or User Notes behavior.
- Did not add OpenAI API, real AI, backend API, Supabase, auth, routing, persistence, scenario generation, source verification, document upload, dashboard or CRM/task-manager behavior.

## AI Demo Presentation Layer Closure

- Closed AI Demo Presentation Layer after plan creation, UI implementation, local smoke, production deploy and production smoke validation.
- Moved `plans/active/2026-06-28-ai-demo-presentation-layer-plan.md` to `plans/completed/2026-06-28-ai-demo-presentation-layer-plan.md`.
- Confirmed production demo is ready at `https://nova-agent-demo.vercel.app/` with AI-assistant presentation while remaining demo-only.
- Confirmed real AI/OpenAI, Supabase, API, auth, routing, persistence, scenario generation, source verification, document upload and dashboard behavior were not added.
- Did not change application code, README, package configuration, specs, AGENTS.md, tests, domain/workflow logic or content repository behavior.

## Voice-First AI Demo Layer Plan

- Created `plans/active/2026-06-28-voice-first-ai-demo-layer-plan.md` to plan a voice-first presentation layer for the Nova Agent demo.
- Recommended Option A, a safer visual voice-first demo with text fallback, for the current MVP; browser speech recognition remains a later gated decision.
- Preserved demo-only boundaries: no real AI/OpenAI, based on the current demonstration scenario, no new scenarios in this version, not legal/medical/tax advice, no official status and no external action confirmation.
- Did not change application code, README, package configuration, specs, Product Principles, Technical Architecture, AGENTS.md, tests, domain/workflow logic, content repository, Supabase, API, auth, routing or persistence behavior.

## Voice-First AI Demo Layer Step 1: UI Implementation

- Updated the first screen to use the planned Option A voice-first visual demo: Nova Agent, `Скажите, что нужно решить`, a central voice/orb element, `Демо голосового режима`, text fallback and an assistant-style demo response.
- Added visible boundaries that voice is not recorded, the response uses the current demonstration scenario, there is no real AI/OpenAI, Nova Agent does not create new scenarios and the demo is not legal, medical or tax advice.
- Kept the existing Action Plan workflow unchanged and did not add microphone permission, browser speech recognition, fake recording, fake listening state, fake waveform, Supabase, API, auth, routing or persistence behavior.

## Voice-First AI Demo Layer: Mobile Overflow Fix

- Fixed horizontal overflow on 390px mobile viewport: `agentic-command-panel` was 400px wide due to `width: 100%` being interpreted as `content-box` width while padding (18px × 2) and border (1px × 2) added to the total.
- Added `box-sizing: border-box` to `.agentic-command-panel` and `.agentic-summary` in `src/app/App.css` so that `width: 100%` includes padding and border in the total width.
- Local headless smoke (Playwright, 390×844) confirmed: `scrollWidth 390 = clientWidth 390`, `maxScrollX 0`, no overflow offenders, desktop and mobile layout unchanged, voice CTA and workflow reachable.
- Did not change domain logic, workflow helpers, tests, component structure, package configuration, Supabase, API, auth, routing or persistence behavior.

## Voice-First AI Demo Layer Closure

- Closed Voice-First AI Demo Layer after plan creation, voice-first UI implementation, mobile overflow fix, production deploy and production visual smoke validation.
- Moved `plans/active/2026-06-28-voice-first-ai-demo-layer-plan.md` to `plans/completed/2026-06-28-voice-first-ai-demo-layer-plan.md`.
- Confirmed the production demo now has voice-first assistant presentation at `https://nova-agent-demo.vercel.app/`, remains demo-only and has no horizontal overflow on the 390px mobile smoke viewport.
- Confirmed real AI/OpenAI, real microphone permission, browser speech recognition, Supabase, API, auth, routing, persistence, scenario generation, source verification and document upload were not added.

## Browser Voice Input Demo Plan

- Created `plans/active/2026-06-28-browser-voice-input-demo-plan.md` to plan browser-side speech-to-text for the existing voice-first demo.
- Scoped the plan to Web Speech API feature detection with `SpeechRecognition` / `webkitSpeechRecognition`, honest unsupported and permission-denied fallbacks, and no package changes.
- Replaced the future voice boundary direction from visual-only copy to browser voice input copy: voice is used only to enter text in this demo and works only in supported browsers.
- Preserved demo-only boundaries: no real AI/OpenAI, no backend API, no Supabase, no auth, no persistence, no scenario generation, no official status and no external action confirmation.
- Did not change application code, README, package configuration, specs, tests, domain/workflow logic or production deployment behavior.

## Browser Voice Input Demo Step 1: Implementation

- Added browser-side voice input behavior to `src/app/components/AgenticDemoShell.tsx` using Web Speech API feature detection for `SpeechRecognition` / `webkitSpeechRecognition`.
- Added supported, unsupported, requesting-permission, listening, result, permission-denied and no-speech/error UI states with manual text fallback.
- Replaced the old visual-only voice copy with honest browser voice input boundaries: voice is used only for text input in this demo and works only in supported browsers.
- Updated `src/app/App.test.tsx` to cover unsupported browser fallback, supported recognition mock result, permission denied fallback, no-speech fallback, manual input and existing workflow reachability.
- Did not add OpenAI API, backend API, Supabase, auth, persistence, package changes, scenario generation, official verification, external actions or domain/workflow logic changes.

## Browser Voice Input Demo Step 1: Cleanup / Robustness Fix

- Fixed the speech recognition lifecycle by storing the active browser recognition instance in a ref, preventing parallel sessions on repeated voice clicks and cleaning up on unmount.
- Added cleanup that removes recognition callbacks and calls `abort()` when available, with `stop()` as fallback, so stale recognition callbacks cannot update UI state after cleanup.
- Added tests for repeated voice click prevention and unmount cleanup while preserving unsupported, permission-denied, no-speech, manual-input and existing workflow coverage.
- Did not change domain logic, workflow logic, content repository, package configuration, README, specs, backend API, Supabase, auth, persistence, scenario generation, official verification or production deployment behavior.

## Browser Voice Input Demo Step 1: Diagnostic Error Copy

- Added user-visible diagnostic speech recognition error codes in `src/app/components/AgenticDemoShell.tsx` so manual Chrome smoke can distinguish `not-allowed`, `audio-capture`, `no-speech`, `network` and other browser recognition failures.
- Added `src/app/App.test.tsx` coverage for `not-allowed`, `audio-capture` and `no-speech` diagnostic copy while preserving manual text fallback and existing workflow reachability.
- Did not add OpenAI, backend API, Supabase, auth, persistence, package changes, scenario generation, official verification or production deployment behavior.

## Browser Voice Input Demo Step 2: Local Real Chrome Voice Smoke

- Recorded that local real browser voice input smoke passed after the user clicked `Говорить`, spoke a phrase and saw the transcript fill the task textarea.
- Confirmed browser speech recognition produced `ich muss Antrag stellen für neugeborene Kinder` and the demo assistant response appeared.
- Manual fallback remains available; production deploy and production smoke are still pending.
- Did not change application code, package configuration, domain/workflow logic, backend API, Supabase, auth, persistence or production deployment behavior.

## Browser Voice Input Demo Step 3: Voice Flow Improvement

- Improved browser voice result UX so a successful final transcript immediately shows the demo assistant response and `Открыть план действий` CTA without requiring a separate `Построить план` click.
- Kept the textarea as editable transcript and manual fallback, added interim transcript handling, accumulated final transcript chunks and added empty/incomplete transcript fallback copy.
- Preserved explicit workflow navigation: voice result does not automatically open the Action Plan without the user pressing `Открыть план действий`.
- Did not add OpenAI, backend API, Supabase, auth, persistence, external search/integrations, scenario generation, official verification, external actions or domain/workflow logic changes.
- Production deploy and production smoke remain pending for this improvement.

## Browser Voice Input Demo Step 4: Local Real Chrome Auto-Submit Smoke

- Recorded that local real Chrome voice auto-submit smoke passed on `f5b9581 fix: auto-submit browser voice transcript`.
- Confirmed the voice transcript filled the task textarea, the demo assistant response appeared automatically and `Открыть план действий` appeared without clicking `Построить план`.
- Production deploy and production smoke remain pending.
- Did not change application code, package configuration, domain/workflow logic, backend API, Supabase, auth, persistence or production deployment behavior.
